from setuptools import setup, find_packages

setup(
    name="image_search",
    version="0.1.0",
    description="image-search utilities for master-ip (editable dev install)",
    packages=find_packages(),
    include_package_data=True,
)
